{
  "name": "voidrift",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "VOIDRIFT — a WebGPU space-exploration graphics tech demo (Babylon.js).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "boot-check": "node scripts/boot-check.mjs",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@babylonjs/core": "^7.34.2"
  }
}
